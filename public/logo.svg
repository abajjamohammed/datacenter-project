<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <radialGradient id="sphereGrad" cx="35%" cy="35%" r="90%">
      <!-- Highlight (Top Left) -->
      <stop offset="0%" stop-color="#60a5fa" />
      <!-- Main Blue (Your Theme Color) -->
      <stop offset="60%" stop-color="#0096FF" />
      <!-- Shadow (Bottom Right) -->
      <stop offset="100%" stop-color="#005a9e" />
    </radialGradient>
  </defs>
  <!-- The Sphere -->
  <circle cx="50" cy="50" r="50" fill="url(#sphereGrad)" />
</svg>